"""Interface layer package for the IAM bounded context.

The interface layer exposes HTTP-facing helpers and Flask Blueprints.  It
translates incoming requests into application-service calls and maps failures
into HTTP responses.
"""
