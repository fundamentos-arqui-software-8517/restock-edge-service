"""Infrastructure layer package for the IAM bounded context.

Infrastructure components contain database models and repository
implementations.  They adapt the domain model to the local SQLite persistence
mechanism used by the edge service.
"""
